# 액션

상세 화면의 액션은 탐색과 반출에만 집중한다. BOM 링크를 직접 고치는 버튼은
MVP에서 제공하지 않는다.

- 기본 액션은 `정전개`, `역전개`, `엑셀 내보내기`, `부품 상세로 이동`이다.
- `엑셀 내보내기`는 현재 루트와 현재 방향을 그대로 사용한다.
- 트리 로딩 중, 오류 상태, 루트 미확정 상태에서는 내보내기 버튼을 비활성화한다.
- `Where-Used`는 별도 화면이 아니라 `역전개` 방향 전환으로 처리한다.
- `BOM 편집`, `링크 추가`, `수량 변경`, `삭제` 버튼은 후속 범위다.
