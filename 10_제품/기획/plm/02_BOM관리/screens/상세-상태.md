# 화면 상태

상세 화면은 읽기 전용이지만, 조회 상태 구분은 명확해야 한다. 사용자는
로딩과 빈 결과, 오류를 혼동하지 않아야 한다.

- `loading`: 루트 요약은 유지하고 트리 영역만 스켈레톤으로 전환한다.
- `empty`: 선택한 방향 기준 관계가 없음을 명시한다.
- `error`: 잘못된 방향값, 네트워크 오류, 권한 오류를 배너로 구분한다.
- `permission denied`: 루트 정보 대신 접근 제한 안내만 노출한다.
- `read only`: 화면 기본 상태이며 별도 경고는 두지 않는다.
