# 목록 테이블

테이블은 루트 후보를 고르는 화면이므로, 트리 깊이보다 `어떤 조립품을
열어야 하는지` 판단할 수 있는 열만 남긴다.

- 기본 컬럼은 `품번`, `품명`, `카테고리`, `상태`, `현재 리비전`,
  `하위 부품 수`, `도면 여부`다.
- 행 클릭은 `BOM 상세`로 이동한다.
- `하위 부품 수`는 `childrenCount`를 그대로 사용한다.
- Obsolete 행은 회색 텍스트 처리하되 숨기지 않는다.
- MVP에서는 행별 `편집`, `삭제`, `일괄 반영` 버튼을 두지 않는다.
