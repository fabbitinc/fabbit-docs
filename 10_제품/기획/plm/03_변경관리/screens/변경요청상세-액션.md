# 변경요청 상세 · 액션

- `DRAFT`에서는 `수정`, `제출`, `닫기`를 노출한다.
- `SUBMITTED`에서는 `반영`, `닫기`, `수정`을 노출한다.
- `MERGED`에서는 수정/상태 전이 CTA를 숨기고 결과 정보만 노출한다.
- `CLOSED`에서는 `다시 열기`만 노출한다.
- 상태 전이 실패 시 상단 배너에 현재 상태와 실패 사유를 함께 표시한다.
- 권한이 없는 사용자는 액션 버튼을 숨기고 읽기 전용 안내만 노출한다.
