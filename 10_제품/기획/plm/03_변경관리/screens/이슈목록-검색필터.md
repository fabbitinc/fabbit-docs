# 이슈 목록 · 검색 / 필터

- 목록 검색은 `title containsIgnoreCase` 기준이다.
- 상태 필터는 `OPEN`, `CLOSED`, `전체` 3개를 제공한다.
- 부품 상세에서 진입한 경우 상단에 `연결 부품` 컨텍스트 칩을 고정 표시한다.
- 검색과 상태 필터는 함께 적용할 수 있다.
- `loading`에서는 필터 값을 유지한 채 결과 영역만 스켈레톤 처리한다.
- `error`에서는 마지막 검색 조건을 유지하고 `다시 시도` 버튼을 노출한다.
