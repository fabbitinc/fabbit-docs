# 변경요청 목록 · 검색 / 필터

- 목록 검색은 제목 부분 일치만 지원한다.
- 기본 상태 필터는 `Issue.state` 기준 `OPEN`, `CLOSED`, `전체`를 제공한다.
- 추가 필터로 `crState`(`DRAFT`, `SUBMITTED`, `MERGED`, `CLOSED`)를 제공한다.
- 두 필터는 AND 조건으로 적용한다.
- `loading`에서는 필터 값을 유지하고 결과 영역만 스켈레톤 처리한다.
- `error`에서는 마지막 필터 조건을 유지한 채 재조회 버튼을 노출한다.
