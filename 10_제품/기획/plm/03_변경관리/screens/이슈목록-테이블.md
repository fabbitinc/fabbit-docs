# 이슈 목록 · 테이블

- 컬럼은 `번호`, `제목`, `상태`, `연결 부품`, `연결 변경요청 수`,
  `댓글 수`, `최종수정일` 순으로 노출한다.
- 행 클릭 시 이슈 상세 화면으로 이동한다.
- 상태 배지는 `OPEN`, `CLOSED` 두 값만 사용한다.
- `empty`에서는 `등록된 이슈가 없습니다`와 `새 이슈 등록` CTA를 함께 노출한다.
- `error`에서는 테이블 대신 오류 배너와 재조회 버튼을 노출한다.
- 목록에서 댓글 본문 미리보기는 제공하지 않고, 댓글 수만 표시한다.
