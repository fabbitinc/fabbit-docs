# 변경요청 상세 · 연결 이슈

- 연결 이슈 영역은 `번호`, `제목`, `상태`, `연결 변경요청 수`를 함께 보여준다.
- 이슈 추가/제거는 lookup picker의 최종 선택 목록을 sync 한다.
- `MERGED`와 `CLOSED` 변경요청에서는 연결 편집을 막고 읽기 전용으로 보여준다.
- 이 영역 하단에는 `MERGE 후 다른 미해결 변경요청이 없으면 이슈 자동 종결`
  안내 문구를 고정 노출한다.
- `empty`에서는 `연결된 이슈가 없습니다`와 `이슈 연결` CTA를 노출한다.
