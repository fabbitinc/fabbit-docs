# 변경요청 목록 · 테이블

- 컬럼은 `번호`, `제목`, `crState`, `검토 현황`, `연결 이슈 수`,
  `연결 부품`, `최종수정일` 순으로 노출한다.
- 검토 현황은 `승인 수 / 대기 수 / 반려 수` 요약으로 표시한다.
- `MERGED`와 `CLOSED`는 서로 다른 배지와 문구로 구분한다.
- 행 클릭 시 변경요청 상세로 이동한다.
- `empty`에서는 `등록된 변경요청이 없습니다`와 `새 변경요청 등록` CTA를 노출한다.
- 목록에서는 리뷰 개별 코멘트 미리보기를 제공하지 않는다.
