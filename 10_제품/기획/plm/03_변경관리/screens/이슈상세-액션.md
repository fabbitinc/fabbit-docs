# 이슈 상세 · 액션

- `OPEN` 상태에서 `변경요청 생성`, `이슈 수정`, `닫기`를 노출한다.
- `CLOSED` 상태에서는 `다시 열기`만 노출한다.
- `변경요청 생성`은 현재 이슈 번호와 연결 부품을 기본값으로 넘긴다.
- 상태 전이 실패 시 상단 배너에 오류 코드를 노출하고 현재 상태를 유지한다.
- 권한이 없는 사용자는 액션 버튼을 숨기고 읽기 전용 안내만 노출한다.
