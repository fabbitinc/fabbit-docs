# 이슈 편집 · 저장 상태

- 기본 CTA는 `저장`, 보조 CTA는 `취소`다.
- 저장 성공 시 같은 화면에 머무르며 `이슈 저장됨` 토스트를 노출한다.
- `VALIDATION_ERROR`: 제목 아래 필드 오류를 표시한다.
- `PERMISSION_DENIED`: 전체 화면을 읽기 전용으로 전환한다.
- `INVALID_STATE`: 상단 배너에 `닫힌 이슈는 수정할 수 없습니다`를 노출한다.
- `saving` 상태에서는 저장 버튼을 비활성화하고 중복 제출을 막는다.
