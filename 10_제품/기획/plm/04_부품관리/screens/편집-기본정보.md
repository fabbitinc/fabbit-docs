# 기본 정보

- 기본 정보 블록은 `품번`, `품명`, `유형`, `카테고리` 편집 영역이다.
- `품번`은 현재 상태와 편집 흐름에 따라 읽기 전용으로 보일 수 있다.
- 필수 필드 표시는 폼 라벨 수준에서만 드러내고, 상세 검증 문구는 메인
  와이어프레임에 길게 적지 않는다.
- Released 기준 편집이라도 사용자는 같은 폼 구조를 보고 Draft에서만 값을
  바꾼다.
