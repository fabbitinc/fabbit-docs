# 저장 상태 🔴 (미개발)

- 저장 영역은 `저장`, `취소` 버튼과 저장 전 검증/상태 피드백 블록으로 구성한다.
- 메인 와이어프레임은 버튼 위치와 오류 영역 배치만 보여주고, 서버 오류 코드
  설명은 정책 문서에서 다룬다.
- `저장 전 검증` 블록은 필수값, 품번 중복, 커스텀 속성 검사를 한눈에
  보여주는 용도다.
- `상태 피드백` 블록은 `loading`, `locked`, `error` 같은 UI 상태가 어디에
  나타나는지 확인하는 용도다.
